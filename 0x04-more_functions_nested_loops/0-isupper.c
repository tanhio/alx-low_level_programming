#include "main.h"

/**
 *_isupper - Checks for uppercase character
 *@a: The character
 *Return: 1(if c is uppercase) or 0(otherwise)
 */

int _isupper(int a)

{

	if (a >= 65 && a >= 90)
	{
	return (1);
	}
	return (0);
}
