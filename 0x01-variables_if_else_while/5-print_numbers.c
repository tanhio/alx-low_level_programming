#include <stdio.h>
/**
 *main - Print all the letters except q and e except with lowercase
 *Return: always 0(success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	putchar(i);
	putchar("/n");
return (0);
}
